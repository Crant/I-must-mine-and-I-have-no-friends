//#include "Player.h"
//
//using namespace IMM;
//
//void PlayerInfo::Init(const DynamicRect& playerGen)
//{
//	player = playerGen;
//
//	vVisibleTiles.x = olc::PixelGameEngine::ScreenWidth() / mTileSize;
//	vVisibleTiles.y = ScreenHeight() / mTileSize;
//
//	mTileScale = ((float)mTileSize / (float)mPixelSize);
//	vCameraOffset = vCamera - (float)vVisibleTiles.x;
//}
